# Revision Log

| Date | Description | By |
|------|-------------|-----|
| 30 Jun 2026 | Chapter 8 revised and expanded. Longitudinal offset and unreachable points content moved from standalone §8.4 into §8.2.1 as new subsections §8.2.1.4, §8.2.1.4.1, and §8.2.1.4.2; all subsequent sections renumbered (former §8.5–§8.9 became §8.4–§8.8). Added §8.2.2 (Stationing and DistanceAlong) and §8.3.3 (Linear Placement Example illustrating sign placement along a gradient curve with `PlacementRelTo`). Figure 8.1.2-1 updated to show all five `IfcPointByDistanceExpression` attributes. Added Figure 8.2.1.3-1 (OffsetVertical) and Figure 8.2.1.4.1-1 (PI unreachable zone). | R. Brice |
| 6 Jul 2026 | Chapter 8 cleanup following the 30 Jun restructuring. Removed the now-unused Figure 8.2.1.3-1 (OffsetVertical) and its dangling §8.2.1.3 cross-reference; §8.2.1.4 renumbered to §8.2.1.1 (Longitudinal Offset and Unreachable Points) with its stale §8.2.1.2/§8.2.1.3/§8.2.1.4 table references corrected. Dropped the §8.2.1.1.1/§8.2.1.1.2 fifth-level headings in favor of bold run-in text; the PI unreachable-zone figure renumbered from 8.2.1.4.1-1 to 8.2.1.1-1 and its file renamed to match. Corrected corresponding entries in `Index.md`. Regenerated Figure 8.2.1-1 (`ifcpointbydisanceexpression_offsets`) from its script in `images/scripts/`. | R. Brice |
| 7 Jul 2026 | Chapter 8 §8.2/§8.3 rewritten to make explicit that `IfcAxis2PlacementLinear` performs two independent jobs (Location via §8.2.1's offsets, Orientation via §8.3's `Axis`/`RefDirection`), and that the offset directions are derived purely from `BasisCurve`'s own tangent, independent of `Axis`/`RefDirection`. Corrected §8.3.2 interpretation 2's default `Axis` definition to anchor on `BasisCurve`'s tangent directly rather than on `RefDirection`. Added a style clarification distinguishing `IfcPointByDistanceExpression.BasisCurve` (the 3D curve) from the horizontal-projection "basis curve" used for `DistanceAlong`. Removed §8.7 (Complete Example, superseded by §8.3.3) and renumbered §8.8 to §8.7; updated `Index.md` accordingly. Converted Figure 8.1.2-2's station/offset/elevation values to Imperial units to match §8.1.1 Cases 1–2, reordered Case 2's station so it falls after Case 1, and fixed its legend arrow directions. Generalized the §8.3.3 worked example from a sign to a generic object. Added checklist item #4 (§8.7) warning that `OffsetVertical` is not a true elevation offset. Added §12.3.4 real-world test case `FHWA_Alignment_LinearPlacement_VerticalOffset.ifc`/`.csv`, contrasting `OffsetVertical` against `PlacementRelTo` at the same 124 stations as the existing Linear Placement Variant. Corrected `generate_3d_csv.py`'s `LINEAR_PLACEMENT_FILES` handling to resolve each `IfcLinearPlacement` directly via `ifcopenshell.util.placement.get_local_placement()` rather than sampling the bare 3D curve at its `DistanceAlong` — the latter only happened to match because `FHWA_Alignment_with_Linear_Placement.ifc`'s placements carry no offsets, so the reference CSV was not actually exercising `IfcLinearPlacement` evaluation as §12.3.4 claims; regenerated `FHWA_Alignment_with_Linear_Placement.csv` accordingly (values unchanged). | R. Brice |
